/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost']
      },
      compiler: {
        styledComponents: true,
      }
  }


module.exports = nextConfig
